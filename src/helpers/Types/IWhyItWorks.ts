import { LucideIcon } from "lucide-react";

export interface PrincipleItem {
    icon: LucideIcon;
    title: string;
    description: string;
    color: string;
    points: string[];
};

export interface TrustItem {
    icon: LucideIcon;
    title: string;
    description: string;
};